#include <bits/stdc++.h>
int main(){
    std::string a;
    std::cin>>a;
    int x=0,n=a.size();
    for(int i=0;i<n;i++){
        if(a[i]!=a[i-1] && a[i]!=a[i+1]){
            
        }
    }
    
    return 0;
}