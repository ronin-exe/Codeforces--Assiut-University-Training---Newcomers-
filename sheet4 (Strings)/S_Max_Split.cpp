#include <bits/stdc++.h>
int main(){
    std::string a,arr[1000];
    std::cin>>a;
    int l=0,r=0;
    for(int i=0;i<a;size();i++){
        if(a[i]=='L'){
            l++;
        }
        else if(a[i]=='R'){
            r++;
        }
        if(r==l)
    }

}